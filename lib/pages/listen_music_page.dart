import 'package:flutter/material.dart';

class ListenMusicPage extends StatelessWidget {
  const ListenMusicPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Listen Music',
        style: TextStyle(fontSize: 24.0),
      ),
    );
  }
}